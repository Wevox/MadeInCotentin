class Sale{
	int idShop, idSale;
	DateTime startDateTime, endDateTime;
	String photo, description, name;

	Sale({this.idShop, this.idSale, this.startDateTime, this.endDateTime, this.photo, this.description, this.name});
}